{
  "private": true,
  "workspaces": [
    "packages/*",
    "actions/*"
  ],
  "scripts": {
    "build": "npm run build -w=@releasy/core",
    "prepublishOnly": "npm run build",
    "dev": "npm run build && ts-node src/index.ts"
  }
}
