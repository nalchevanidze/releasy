{
  "name": "relasy",
  "version": "0.1.6",
  "private": true,
  "workspaces": [
    "packages/*",
    "actions/*"
  ],
  "scripts": {
    "build:core": "npm -w @relasy/core run build",
    "build:action": "npm -w @relasy/draft-release-action run build && npm -w @relasy/publish-release-action run build",
    "build:cli": "npm -w @relasy/cli run build",
    "build": "npm run build:core && npm run build:action && npm run build:cli"
  }
}
