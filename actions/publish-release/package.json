{
  "name": "@relasy/publish-release-action",
  "private": true,
  "version": "2.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@octokit/rest": "^22.0.1",
    "@relasy/actions-common": "workspace:*",
    "@relasy/core": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.0.0",
    "vitest": "^0.24.2"
  }
}
